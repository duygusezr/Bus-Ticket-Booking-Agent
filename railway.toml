# Railway build ayarları
[build]
# Dockerfile kullan (Railpack yerine)
dockerfilePath = "Dockerfile"

[deploy]
# Railway PORT ortam değişkenini otomatik kullanır
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
