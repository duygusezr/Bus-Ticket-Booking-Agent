# ── Gerekli bağımlılıklar ──────────────────────────────────────────────────
# Sürümler pinlendi — reproducible builds için.
# Güncellemek için: pip install -U <paket> ve bu dosyayı güncelle.
# ──────────────────────────────────────────────────────────────────────────

fastapi==0.115.6
uvicorn[standard]==0.34.0
edge-tts==7.2.8
python-dotenv==1.1.0
python-multipart==0.0.20
websockets==14.2
httpx==0.28.1
watchfiles==1.0.4
openai==1.82.0

# ── PostgreSQL dual-write desteği ──────────────────────────────────────────
asyncpg==0.30.0

# ── İsteğe bağlı: Semantik önbellek ───────────────────────────────────────
# Semantic cache şu an devre dışı. Etkinleştirmek için services/ dizininde
# yeni bir cache servisi yaz ve bu bağımlılıkları requirements.txt'e ekle:
#   numpy>=1.26
#   sentence-transformers>=3.0
# ──────────────────────────────────────────────────────────────────────────
